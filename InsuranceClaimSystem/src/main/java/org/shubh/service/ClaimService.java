package org.shubh.service;

import org.shubh.Claims;

public class ClaimService {

//    make method ValidateClaim(Claims claim) to validate the claim based on the policy details and claim status.
    public boolean validateClaim(Claims claim) {
        // Implement validation logic based on policy details and claim status
        // For example, check if the claim date is within the policy period and if the claim status is valid
        return true; // Return true if the claim is valid, otherwise return false
    }


}
